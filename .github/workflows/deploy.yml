# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_deploy --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: deploy

on:
  push:
    branches:
      - release

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Deploy'
        run: ./build.cmd Deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: Portraits_in_Brick_and_Time_-_Brine_And_Coin.exe'
        uses: actions/upload-artifact@v5
        with:
          name: Portraits_in_Brick_and_Time_-_Brine_And_Coin.exe
          path: publish-win/Portraits_in_Brick_and_Time_-_Brine_And_Coin.exe
      - name: 'Publish: Portraits_in_Brick_and_Time_-_Brine_And_Coin.appimage'
        uses: actions/upload-artifact@v5
        with:
          name: Portraits_in_Brick_and_Time_-_Brine_And_Coin.appimage
          path: publish-linux/Portraits_in_Brick_and_Time_-_Brine_And_Coin.appimage
